# tad-vetor
O vetor é um tipo abstrato de dados que determina que a manipulação dos dados ocorra através de índices numéricos, assim como no array.

Por isso, talvez seja da implementação mais natural usar um array para armazenar os dados do vetor.